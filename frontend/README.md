# Grafinet Frontend

React + Vite frontend for IP routing visualization.

## Run

1. Install dependencies:
   - npm install
2. Start dev server:
   - npm run dev
3. Optional API URL:
   - set VITE_API_BASE_URL=http://localhost:8000/api
